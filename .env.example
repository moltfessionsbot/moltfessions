# Docker Compose environment
# Copy to .env and fill in values

# PostgreSQL credentials (used by docker-compose)
POSTGRES_USER=moltfessions
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
POSTGRES_DB=moltfessions
